# ═══════════════════════════════════════════════════════════════
# KrishiMitra AI — Python Requirements v2.1
# Install: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════

# ── Django core ────────────────────────────────────────────────
Django>=5.2,<6.0
djangorestframework>=3.15.0
django-cors-headers>=4.3.0
django-filter>=24.0
drf-spectacular>=0.27.0         # OpenAPI schema + Swagger UI
djangorestframework-simplejwt>=5.3.0

# ── Production server ──────────────────────────────────────────
gunicorn>=22.0.0
whitenoise>=6.7.0               # Static file serving (no Nginx needed for API-only)

# ── Database ───────────────────────────────────────────────────
psycopg2-binary>=2.9.9          # PostgreSQL adapter
dj-database-url>=2.2.0          # DATABASE_URL → Django DATABASES dict

# ── Environment / Config ───────────────────────────────────────
python-decouple>=3.8

# ── HTTP & External APIs ───────────────────────────────────────
requests>=2.32.0
urllib3>=2.2.0                  # Fixed: no longer pinned to <2.0

# ── Google Gemini AI (chatbot grounding) ───────────────────────
google-generativeai>=0.8.0

# ── Data processing ────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
python-dateutil>=2.9.0
pytz>=2024.1

# ── Geospatial / Location ──────────────────────────────────────
geopy>=2.4.0                    # Haversine, geocoding helpers

# ── Web scraping (mandi data fallbacks) ────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.2.0

# ── Security ───────────────────────────────────────────────────
cryptography>=42.0.0

# ── Monitoring ─────────────────────────────────────────────────
sentry-sdk[django]>=2.0.0
psutil>=6.0.0                   # System metrics for health endpoint

# ── Caching / Tasks (optional — only needed with Redis) ────────
redis>=5.0.0
celery>=5.4.0
django-redis>=5.4.0
django-celery-beat>=2.6.0

# ── Text-to-Speech (optional — SMS/IVR feature) ────────────────
gTTS>=2.5.0

# ── File type detection (KrishiRaksha image validation) ────────
python-magic>=0.4.27

# ── Scikit-learn (crop ML scoring helpers) ─────────────────────
scikit-learn>=1.5.0
joblib>=1.4.0
