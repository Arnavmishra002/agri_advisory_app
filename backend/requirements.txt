# ═══════════════════════════════════════════════════════════════
# KrishiMitra AI — Python Requirements v2.1
# Install: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════

# ── Django core ────────────────────────────────────────────────
Django>=5.2,<6.0
djangorestframework>=3.15.0,<3.18
django-cors-headers>=4.3.0,<5
django-filter>=24.0,<27
drf-spectacular>=0.27.0,<0.31         # OpenAPI schema + Swagger UI
djangorestframework-simplejwt>=5.3.0,<6

# ── Production server ──────────────────────────────────────────
gunicorn>=22.0.0,<27
whitenoise>=6.7.0,<7               # Static file serving (no Nginx needed for API-only)

# ── Database ───────────────────────────────────────────────────
psycopg2-binary>=2.9.9          # PostgreSQL adapter
dj-database-url>=2.2.0          # DATABASE_URL → Django DATABASES dict

# ── Environment / Config ───────────────────────────────────────
python-decouple>=3.8
python-dotenv>=1.0.0            # .env auto-loading in manage.py / wsgi.py

# ── HTTP & External APIs ───────────────────────────────────────
requests>=2.32.0,<3
urllib3>=2.2.0,<3                  # Fixed: no longer pinned to <2.0

# ── Google Gemini AI (chatbot grounding) ───────────────────────

# ── Data processing ────────────────────────────────────────────
numpy>=1.26.0,<2.0
pandas>=2.2.0,<3.0
python-dateutil>=2.9.0
pytz>=2024.1

# ── Geospatial / Location ──────────────────────────────────────
geopy>=2.4.0                    # Haversine, geocoding helpers

# ── IoT Sensor Integration (Phase 2) ───────────────────────────
paho-mqtt==1.6.1

# ── Web scraping (mandi data fallbacks) ────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.2.0

# ── Security ───────────────────────────────────────────────────
cryptography>=42.0.0

# ── Monitoring ─────────────────────────────────────────────────
sentry-sdk[django]>=2.0.0
psutil>=6.0.0                   # System metrics for health endpoint

# ── Caching / Tasks (optional — only needed with Redis) ────────
redis>=5.0.0
celery>=5.4.0
django-redis>=5.4.0
django-celery-beat>=2.6.0

# ── Text-to-Speech (optional — SMS/IVR feature) ────────────────
edge-tts>=7.2.8,<8

# ── File type detection (KrishiRaksha image validation) ────────
python-magic>=0.4.27
Pillow>=10.0.0

# ── Local LLM / Phase 1 RAG service ─────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
chromadb>=0.5.0,<0.6.0             # Compatible with the checked-in Chroma 0.5 store
click>=8.3.3                        # PYSEC-2026-2132 fix; Python 3.10+ runtime

# ── Scikit-learn (crop ML scoring helpers) ─────────────────────
scikit-learn>=1.5.0,<2.0
joblib>=1.4.0

# ── Crop-disease model serving (low memory) ─────────────────────
# tflite-runtime is ~5 MB against ~2 GB for full TensorFlow, so the free-tier
# web host can serve the EfficientNet-B3 disease model. Training still uses
# TensorFlow (requirements-ml.txt); production only ever runs the .tflite
# export produced by the same training run.
tflite-runtime>=2.14.0; python_version < "3.12"
